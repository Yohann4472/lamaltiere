{% extends "LaMaltiereSiteWebMaltiereBundle:Templates:layoutAdministration.html.twig" %}{% block modele %}	<div class="modele">            <h2>Types de sites<span class="action"><a id="TypeSiteNouveau" href="#type"><img src="{{ asset('bundles/lamaltieresitewebmaltiere/images/icones/document--plus.png') }}" width="16px" height="16px" title="Nouveau" alt="Nouveau" /></a></span></h2>                        <p>                <form id="formTypeSiteNouveau" method="post" action="{{ path('lamaltiere_administration_type_site_enregistrer') }}">                    <fieldset>                        <div>                            <label for="libelleFr">Nom du type (Français) : </label>                            <input type="text" required name="libelleFr" id="libelleFr" size="70" />                        </div>                        <div>                            <label for="libelleEn">Nom du type (Anglais) : </label>                            <input type="text" required name="libelleEn" id="libelleEn" size="70" />                        </div>                        <input type="submit" value="Enregistrer">                    </fieldset>                </form>            </p>                        {% for type in liens %}                <h4 class="gras">{{type.nom_type_fr }}<span class="action"><a id="TypeSite{{ type.id }}" href="#modifier{{type.id}}"><img src="{{ asset('bundles/lamaltieresitewebmaltiere/images/icones/document--pencil.png') }}" width="16px" height="16px" title="Modifier" alt="Modifier" /></a><a href="{{ path('lamaltiere_administration_type_site_supprimer', { id : type.id }) }}"><img src="{{ asset('bundles/lamaltieresitewebmaltiere/images/icones/document--minus.png') }}" width="16px" height="16px" title="Supprimer" alt="Supprimer" /></a></span></h4>                <p>                    <form id="formTypeSite{{ type.id }}" method="POST" action="{{ path('lamaltiere_administration_type_site_enregistrer') }}">                        <fieldset>                            <input type="hidden" id="id" name="id" value="{{ type.id }}" />                            <div>                                <label for="libelleFr">Nom du type (Français) : </label>                                <input type="text" required id="libelleFr" name="libelleFr" size="70" value="{{ type.nom_type_fr }}" />                            </div>                            <div>                                <label for="libelleEn">Nom du type (Anglais) : </label>                                <input type="text" required id="libelleEn" name="libelleEn" size="70" value="{{ type.nom_type_en }}" />                            </div>                            <input type="submit" value="Enregistrer">                        </fieldset>                    </form>                </p>            {% endfor %}                        <h2>Sites<span class="action"><a id="SiteNouveau" href="#site"><img src="{{ asset('bundles/lamaltieresitewebmaltiere/images/icones/document--plus.png') }}" width="16px" height="16px" title="Nouveau" alt="Nouveau" /></a></span></h2>                        <p>                <form id="formSiteNouveau" method="post" action="{{ path('lamaltiere_administration_site_enregistrer') }}">                    <fieldset>                        <div>                            <label for="nom">Nom du site : </label>                            <input type="text" required id="nom" name="nom" size="70" value="" />                        </div>                        <div>                            <label for="adresse">Adresse du site : </label>                            <input type="text" id="adresse" name="adresse" size="70" value="" />                        </div>                        <div>                            <label for="lstSitesAdmin">Type : </label>                            <select name="lstSitesAdmin" id="lstSitesAdmin">                                {% for type in liens %}                                    <option value="{{type.id}}">{{ type.nom_type_fr }}</option>                                {% endfor %}                            </select>                        </div>                        <input type="submit" value="Enregistrer">                    </fieldset>                </form>            </p>                        {% for type in liens %}                <h2>{{type.nom_type_fr }}</h2>                {% for site in type.sites %}                    <h4 class="gras">{{site.nom |slice(0, 60) }}<span class="action"><a id="Site{{ site.id }}"  href="#modifier{{ site.id }}"><img src="{{ asset('bundles/lamaltieresitewebmaltiere/images/icones/document--pencil.png') }}" width="16px" height="16px" title="Modifier" alt="Modifier" /></a><a href="{{ path('lamaltiere_administration_site_supprimer', { id : site.id }) }}"><img src="{{ asset('bundles/lamaltieresitewebmaltiere/images/icones/document--minus.png') }}" width="16px" height="16px" title="Supprimer" alt="Supprimer" /></a></span></h4>                    <p>                        <form id="formSite{{ site.id }}" method="post" action="{{ path('lamaltiere_administration_site_enregistrer') }}">                            <fieldset>                                <input type="hidden" id="id" name="id" value="{{ site.id }}" />                                <div>                                    <label for="nom">Nom du site : </label>                                    <input type="text" required id="nom" name="nom" size="70" value="{{ site.nom }}" />                                </div>                                <div>                                    <label for="adresse">Adresse du site : </label>                                    <input type="text" id="adresse" name="adresse" size="70" value="{{ site.adresse }}" />                                </div>                                <div>                                    <label for="lstTypeSitesAdmin">Type : </label>                                    <select id="lstTypeSitesAdmin" name="lstTypeSitesAdmin">                                        {% for type in liens %}                                            {% if type.id == site.type.id %}                                                <option selected value="{{type.id}}">{{ type.nom_type_fr }}</option>                                            {% else %}                                                <option value="{{type.id}}">{{ type.nom_type_fr }}</option>                                            {% endif %}                                        {% endfor %}                                    </select>                                </div>                                <input type="submit" value="Enregistrer">                            </fieldset>                        </form>                    </p>                {% endfor %}            {% endfor %}	</div>        <script>            $("form").each(function(){                $(this).hide();            });            $("a").click(function(){                var currentId = $(this).attr('id');                if($('#form' + currentId + '').is(":visible")){                    $('#form' + currentId + '').hide();                }else{                    $('#form' + currentId + '').show();                }            });        </script>{% endblock %}	